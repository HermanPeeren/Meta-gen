{
  "_comment": [
    "Copy this to cypress.env.json and fill it in. That file is git-ignored,",
    "because it holds a login.",
    "",
    "baseUrl is the Joomla site Meta-gen is installed on. adminUser and",
    "adminPassword are an account that can reach the component."
  ],
  "baseUrl": "http://localhost/Meta-gen/joomla",
  "adminUser": "",
  "adminPassword": ""
}
